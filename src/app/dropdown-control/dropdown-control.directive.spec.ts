import { DropdownControlDirective } from './dropdown-control.directive';

describe('DropdownControlDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownControlDirective();
    expect(directive).toBeTruthy();
  });
});
